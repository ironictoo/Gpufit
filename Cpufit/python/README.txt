Python binding for Cpufit, the CPU implementation of the Gpufit fitting API.

This package provides ctypes-based access to the Cpufit shared library and
mirrors the same `fit` / `fit_constrained` call pattern used by pyGpufit.
